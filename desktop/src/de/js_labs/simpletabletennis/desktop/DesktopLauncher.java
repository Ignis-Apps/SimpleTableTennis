package de.js_labs.simpletabletennis.desktop;

import com.badlogic.gdx.backends.lwjgl.LwjglApplication;
import com.badlogic.gdx.backends.lwjgl.LwjglApplicationConfiguration;

import java.awt.Dimension;
import java.awt.Toolkit;

import de.js_labs.simpletabletennis.SimpleTableTennis;

public class DesktopLauncher {
	public static void main (String[] arg) {
		/*Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();
		LwjglApplicationConfiguration config = new LwjglApplicationConfiguration();
		config.width = screenSize.width;
		config.height = screenSize.height;
		config.title = SimpleTableTennis.TITEL;
		config.resizable = false;
		new LwjglApplication(new SimpleTableTennis(), config);*/
	}
}
